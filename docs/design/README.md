# 设计记录

**状态**：当前有效
**定位**：保留仍被 live 代码、状态文档或路线图引用的设计记录。
**不包含**：未落地且已偏离当前方向的旧方案、只服务历史讨论的草稿、已经没有共享依赖的阶段性设想。

## 当前保留文件

1. [RFC-001-Context-Retrieval.md](./RFC-001-Context-Retrieval.md)
   - 记录 `dimc down` 两阶段设计与导出目录智能扫描方案。
   - 其核心方案已被 live 代码、状态文档和路线图吸收。

## 维护规则

1. `docs/design/` 只保留仍被 live 代码或 live 共享文档引用的设计记录。
2. 若某设计稿未落地、且已不再代表当前方向，应直接从当前树移除，历史追溯交给 git history。
3. 若某设计稿已被 live 实现吸收，但仍需要保留设计背景，应在文首明确写出它是“设计记录”还是“当前规范”。
