# Dimcause V6.0 开发路线图

**创建日期**: 2026-02-10  
**最后更新**: 2026-03-19（Task 083：PROPOSALS 角色收口）
**理论基础**: DIKW 模型 + 本体论  
**相关文档**: [DEV_ONTOLOGY.md](../V6.0/DEV_ONTOLOGY.md)

---

## 当前状态

| Phase | 内容 | 状态 | 代码验证 |
|:---|:---|:---|:---|
| Phase 1 | 本体定义 | ✅ 完成 | `ontology.py` + `ontology.yaml` + 测试 19/19 通过 |
| Phase 2 | 因果推理引擎 | ✅ 完成 | Hybrid Engine + Validator + CLI Graph |
| Phase 3 | 存储与交互 | ✅ 完成 | SQLite Registry 已落地，Pickle 已移除，TUI 已实现 |
| Phase 4 | 存储升级 | ✅ 完成 | P4.0 (Eval), P4.1 (JSON-LD), P4.2 (Vector) 完成 |
| Phase 5 | 架构标准化 | ✅ 完成 | V6.2 六层架构定义与代码对齐 (Alignment Proof) |

---

## Phase 1: 本体定义 ✅ 完成

### 交付物（已验证）
- [x] `docs/V6.0/DEV_ONTOLOGY.md` — 理论文档 [代码已存在]
- [x] `src/dimcause/core/ontology.yaml` — Schema 定义 [代码已存在]
- [x] `src/dimcause/core/ontology.py` — 加载器 [代码已存在]
- [x] `tests/core/test_ontology.py` — 单元测试 19/19 通过 [代码已存在]

### 本体内容
- [x] 6 个类：Requirement, Decision, Commit, Function, Incident, Experiment
- [x] 7 个关系：implements, realizes, modifies, triggers, validates, overrides, fixes
- [x] 3 个公理：commit_must_have_cause, no_decision_cycle, function_traceability
- [x] JSON-LD @context 定义

---

## Phase 2: 因果推理引擎 ✅ 完成


### 2.1 Event 模型重构
- [x] 设计 `SemanticEvent` 模型（继承 `Event`） [已实现]
- [x] 实现 `CausalLink` 数据类 [已实现]
- [x] 添加 `uri` 字段（`dev://类型/hash` 格式） [已实现]
- [x] 添加 `causal_links: List[CausalLink]` 字段 [已实现]
- [x] 添加 `context: dict`（JSON-LD） [已实现]
- [x] 保持向后兼容（`related_events` 作为 `@property`） [已实现]

### 2.2 数据库迁移
- [x] 设计 SQLite schema 升级（v2 → v3） [已实现]
- [x] 创建 `causal_links` 表 [已实现]
- [x] 实现 `dimc migrate v3` 命令 (集成于 `EventIndex.add`) [已实现]
- [x] 编写迁移测试 (`test_event_index_links.py`) [已实现]

### 2.3 因果推理引擎
- [x] 创建 `src/dimcause/reasoning/` 目录 [已实现]
- [x] 实现 `engine.py` (原 planned `causal_engine.py`) [已实现]
  - [x] `infer_by_time()` — 基于时间窗口启发式 (Skeleton)
  - [x] `infer_by_llm()` — 基于 DeepSeek 推理（可选） [已实现: LLMLinker]
  - [x] `manual_link()` — 手动标注 (Via CLI)
- [x] 实现 `ontology_validator.py` (Renamed to `validator.py`) [已实现]
- [x] 实现 `axiom_checker.py` (Renamed to `validator.py`) [已实现]

### 2.4 新 CLI 命令
- [x] `dimc graph link <source> <target> --relation <rel>` [已实现]
- [x] `dimc graph build` — 重建因果图谱 [已实现 (集成 GraphStore Phase 3)]
- [x] `dimc graph query <entity> --depth N` [已实现]
- [x] `dimc graph show` — 可视化 DAG 子图 [已实现]

### 2.5 测试
- [x] 单元测试：因果引擎逻辑 [已实现]
- [x] 集成测试：CLI 端到端 (`test_p2_e2e_integration.py`) [已实现]
- [ ] 性能测试：10 万事件图构建 [Future Work: 待构建大数据集]

---

## Phase 3: 存储与交互 (Storage & Interaction) ✅ 完成

### 3.1 Layer 2 存储 (Persistence) - GraphStore
- [x] **Specs**: 定义 `GraphStore` 接口与 Schema (`graph_nodes`, `graph_edges`) [已实现]
- [x] **Migration**: 实现 `dimc migrate v4` (SQLite Schema Upgrade) [已实现]
- [x] **Impl**: 实现 `GraphStore` 类 (CRUD, NetworkX I/O w/ Pickle Cache) [已实现]
- [x] **CLI**: 集成到 `dimc graph` 命令 [已实现]
- [x] **Verify**: 单元测试 + 集成测试 (Strict Schema Check) [已实现]

### 3.2 交互式 TUI (Exploration)
- [x] 实现 `dimc graph explore` (Textual App) [已实现]

---

## Phase 4: 存储升级 ✅ 完成

### 现状
- GraphStore 使用 `NetworkX` + `SQLite` 持久化 (Registry Mode)
- 查询使用 BFS（无 PageRank）
- 已集成 SQLite Registry & Vector Store 
- [x] **P4.0: 存储方案评估 (Storage Evaluation)**
    - [x] 评估图数据库方案 (NetworkX vs SQLite vs Kùzu) [决策: 保留 NetworkX + SQLite] <!-- id: 24 -->
    - [x] 设计迁移路径 [无需迁移] <!-- id: 25 -->
- [x] **P4.1: 数据互通 (Interoperability)**
    - [x] 实现 JSON-LD 导出功能 (Schema.org/Structure) <!-- id: 26 -->
    - [x] 验证跨 Agent 数据交换 <!-- id: 27 -->
- [x] **P4.2: 向量存储与语义搜索 (Vector & Semantic Search)**
    - [x] 验证 `VectorStore` 初始化与模型下载 <!-- id: 28 -->
    - [x] 验证 SQLite-Vec 存储与检索 <!-- id: 29 -->
    - [x] 集成到 CLI (`dimc search`) <!-- id: 30 -->
- [x] **Technical Debt**: 移除 `graph.pkl`，改为直接从 SQLite 加载 Graph (Completed in Phase 3 cleanup)
- [x] 设计迁移路径 (No additional migration needed)

---

## 基础设施变更记录（已完成）

| 变更 | 状态 | 日期 |
|:---|:---|:---|
| 历史品牌迁移到 DIMCAUSE | ✅ 完成 | 2026-02-10 |
| 仓库迁移到 `dimc` | ✅ 完成 | 2026-02-10 |
| 双 CLI 入口 `dimcause` / `dimc` | ✅ 完成 | 2026-02-10 |

---

## Release 清单

- [ ] 发布 `dimcause` v0.1.0 至 PyPI（当前尚未发布）
- [x] 确认 `README.md` 开源内容完整
- [x] 确认 `README.md` / `README_zh-CN.md` 的安装说明与 live 发布事实一致（当前为源码安装，不宣称 PyPI 已可用）
- [x] 确认 `.gitignore` 排除内部文档
- [x] 配置静态分析 source root，消除 `src` 布局下的一阶导入误报（仓库根目录已新增 `.pyre_configuration`，`pyproject.toml` 中的 `mypy_path` 也已指向 `src`）

## Phase 6.0 Extension: Multi-Agent Support (Critical)
**目标**: 实现多 Agent 并发安全与上下文共享 (基于 RFC-002)。
- [x] **Concurrency**: 基于 `FileLock` 的原子会话创建 (`state.py`)
- [x] **Identity**: `DIMC_AGENT_ID` 配置与日志记录
- [x] **Context**: 全局上下文聚合 (User + Agent A + Agent B)

## Competitive Response (vs Existing Solutions)
- [x] **[P0] MCP Server 差异化端点** (2026-02-16)
    - `get_causal_chain`, `audit_check`, `get_graph_context` 3 个新端点
    - HTTP transport 双模式 (`dimc mcp serve --transport http`)
    - 8/8 单元测试通过
- [x] **[P1] Text/Mermaid Graph** (已有 `dimc graph show --format mermaid`)

---

## V6.1: Production Polish (审计修复与 Production Ready)

**状态**: 🔄 进行中

### 已完成
- [x] **任务 1.1**: 消除僵尸 ChromaDB 依赖 (VectorStore SQLite 迁移)
- [x] **任务 1.1.1**: Reranker 模型缓存修复 (China Mirror + Snapshot Path)
- [x] **任务 1.1.2**: 模型内存管理 "用完即释放" (RT-000 §4.2)
    - `VectorStore.release_model()` — Embedding 释放
    - `Reranker.release_model()` — Reranker 释放 + Singleton 重置
    - `SearchEngine.search()` — 协调生命周期 (峰值 5.87 GB → 3.6 GB)
- [x] **任务 1.4**: 修复 Unknown EventType (Plan C)
    - `scripts/fix_unknown_events.py` (Retroactive Fix)
    - `BasicExtractor` 增强 (Preventative Fix)
- [x] **任务 1.5**: 交互式搜索体验 (UX Upgrade)
    - `dimc search` -> Interactive Loop (View / Trace / Open)
- [x] **任务 1.6**: Session 日志聚合 (Session-End Logic)
    - `dimc down` 自动扫描 `jobs/` 并聚合总结
    - 自动检测 `AG_Exports`，优化交互体验
- [x] **P0-Security**: 移除 `chromadb` 依赖 (Pyproject Clean)
- [x] **RFC-001**: 两阶段 dimc down 交互体验优化 (Session.lock + 精准时间窗匹配)
- [x] **任务 007-01**: Causal Core 时空硬锁与本体关系交叉路由 (2026-02-26)
    - 图库彻底隔绝 `add_relation`，区隔 `add_structural_relation` 和私有连边
    - `CausalEngine` 部署时间锥防线与拓扑孤岛拦截
    - `ExtractionPipeline` 引入 `CAUSAL_RELATIONS_SET` 增量 18 项全绿

- [x] **任务 007-02**: ExtractionPipeline 适配 Causal Core 防线 (2026-02-28)
    - 在 `_make_event` 和 `_run_l2` 阶段注入 `session_id` 至 `Event.metadata`
    - 保障增量测试 `test_extraction_pipeline.py` 100% 通过
- [x] **任务 3.1 Prep**: 隔离历史遗留测试 (Quarantine Legacy Debt) (2026-02-28)
    - 引入 `pytest.mark.legacy_debt` custom marker
    - 隔离 24 个历史报错文件 (103 Failed + 15 Errors)
    - 恢复剩余 650+ 测试套件至 100% Passed 绿灯状态

- [x] **Task 010**: L1 数据防线自动化 (2026-03-01)
    - 实现 `DirectoryImporter` 后台任务注册包装
    - 将搜刮管道挂载到 Orchestrator 常驻调度中

- [x] **CI 契约校验**: 契约签名自动化验证 + L1 不可变性检查 (2026-02-28)
    - `scripts/verify_contracts.py`: AST 解析源码校验 10 个契约函数签名
    - `scripts/check.zsh`: 集成契约校验 + L1 文件不可变性物理闸门
    - `docs/api_contracts.yaml`: 修正所有 module 字段为完整 Python 包路径

- [x] **Git Pre-commit Hook**: 物理阻止在 main 上直接 commit (2026-02-28)
    - `scripts/hooks/pre-commit`: 检测当前分支，main 上 commit 直接 exit 1
    - `scripts/hooks/install.sh`: 一键安装脚本

- [x] **Task 082**: 存储架构文档双轨收口 (2026-03-19)
    - `docs/STORAGE_ARCHITECTURE.md` 已升格为唯一正式有效的存储架构入口
    - 第一层索引与相关 proposal 的存储职责引用链已统一切回正式文档
- [x] **Task 083**: PROPOSALS 角色收口 (2026-03-19)
    - `docs/CORE_OBJECT_MODEL.md` 与 `docs/EVIDENCE_POLICY_AND_CAUSALITY_GRADES.md` 已升格为正式产品子规范
    - `docs/PROPOSALS/STORAGE_ARCHITECTURE_DRAFT_V1.md` 已从 current tree 删除
    - 正式入口与项目落地文档口径已统一到新路径
- [x] **Task 084**: PROPOSALS 第二轮收口 (2026-03-19)
    - `docs/PROPOSALS/README.md` 已建立专项文档区目录入口与准入规则
    - 当前 5 份保留文档已补齐 `状态 / 归属 / 类型` 元数据
    - `ARCHITECTURE_INDEX.md` 已把 `docs/PROPOSALS/` 从“说明性目录”收紧为“有明确准入规则的专项文档区”
- [x] **Task 085**: 实现原则抽升收口 (2026-03-19)
    - 已从三份第二层实现文档中抽升长期稳定原则到第一层正式文档
    - 第一层与第二层之间已明确切开“长期产品语义”与“当前版本实现决策”
    - `Design / Contract / ADR` 不再被当成固定层级标签，而是按内容职责归层

- [x] **Anti-Forgetting Protocol**: 三层对话遗忘防御 (2026-02-28)
    - 任务启动前强制 Checklist（确认分支 + 重读核心规则）
    - 任务完成后写入检查点（分支/进度/违规记录）
    - 长对话超 15 轮任务主动预警 `[CONTEXT_DECAY]`

### 🚀 V6.1 - V6.2 执行优先级路线 (Execution Plan)

> 基于架构审阅重塑的三阶段计划，按严格的依赖与质量红线顺序执行。

**第一阶段：稳固地基与质量防线 (Priority: P0-P1)**
1. **任务 3.1 & 3.2**: 全量测试红线清理 (消除 legacy_debt) + Pyre2 静态分析配置修复 (消除虚假报错)。
    *说明：测试与类型检查是所有后续重构的安全网，必须最优先执行。*
1.1 **Skipped 测试债清理**: 当前全量 `source .venv/bin/activate && pytest -q -rs` 为 `1122 passed, 17 skipped, 4 deselected`；受保护测试已迁出默认套件（`--run-protected` 显式运行），下一步聚焦未实现测试补齐。详见 [`SKIPPED_TESTS_REMEDIATION_PLAN.md`](./SKIPPED_TESTS_REMEDIATION_PLAN.md)。
2. **信任梯度 (Risk Level)**: 已落地到 Task Packet 模板、scheduler frontmatter 解析与 closeout 风险门禁。
3. **任务 1.7**: MCP Server 配置修复已完成；`dimc mcp serve` 已有 transport 参数校验与 CLI smoke test，后续若继续推进，只剩特定外部客户端接入验证，不再构成配置阻断。

**第二阶段：本体知识层补齐 (Priority: P0)**
4. **L3 本体防波堤收口 (SchemaValidator)**: 在已接入运行时校验、治理报告与拒绝写入观测的基础上，继续压缩 legacy 类型生产面。

**第三阶段：基础设施真实化与发布 (Priority: P1-P2)**
5. **L0 调度底座生产化收口 (Orchestrator)**: 在已具备 run/loop/worktree/session/codex-run/closeout 的基础上继续做生产化策略收口。
6. **任务 1.9**: 发布文档与 release 清单核销。✅ 已完成；README 已按 live 发布事实改为源码安装，PyPI 发布继续单列在 Release 准备中跟踪。
8. **Release 准备**: PyPI 发布 + .gitignore 清理。

*(L4 推理引擎补齐作为探索性任务，置于上述核心依赖完成后)*

### 2026-03-17 执行注记

- `dimc why` 已补最小对象证据显示：当事件 `metadata.object_projection` 存在时，CLI 会输出“对象证据区”，至少展示 `Material` 与首条 `Claim.statement`。
- `DecisionAnalyzer` 已补对象证据接线：解释层 prompt 现在会显式携带 `Material` 与 `Claim`，要求叙事说明证据从何而来，而不只复述 Git 时间线和因果链标签。
- 已完成一轮全量 skipped 清理推进：当前 `source .venv/bin/activate && pytest -q -rs` 为 `1122 passed, 17 skipped, 4 deselected`；过时 skipped 和“依赖缺失类 skipped”均已清零，且已落地 `data_pipeline` 三条与 `fault_tolerance` 两条可执行用例，剩余 `17` 个 skipped 已按手工/未实现分组，另有 `4` 个受保护测试默认 deselect。
- 去假整改进展：`tests/test_e2e_scenarios.py::test_audit_scan` 已从 fake `run_audit` 改为受保护真实 audit 链路验证（临时工作区 + 真实检查执行）。
- 因此，`why` 当前剩余工作已进一步收敛到排序、压缩、展示 polish 与更深的对象化统一，不再是“对象证据完全未进入解释链”的缺口。
- `risk_level` 已落地到 Task Packet 模板、scheduler task card、`summary/closeout` 风险门禁和前置脚本解析；低风险任务才进入默认自动收口候选。
- `dimc mcp serve` 当前已支持 `stdio/http` 两种传输，非法 transport 会直接报错；`tests/test_mcp.py`、`tests/test_mcp_server.py` 与 `tests/test_cli_mcp.py` 为 `16/16` 通过；`任务 1.7` 不再属于待修配置项。
- 仓库根目录已新增 `.pyre_configuration`，`source_directories/search_path` 均指向 `src`；`pyproject.toml` 中的 `mypy_path` 也已对齐到 `src`。`任务 3.2` 的配置缺口已收口。当前缺的不是 source root，而是两件事：开发机若要跑 Pyre 需自行安装 `pyre/pyre2/pyrefly`，而 live `mypy` 检查仍有真实类型错误待清。

---

### 待完成
- [x] **任务 1.2**: 废弃 GraphStore.save() (P0 API Risk)
- [x] **任务 1.3**: BFS 逻辑 Bug 修复与性能优化 (10k 节点 Capped BFS 截断 - Task 011)
- [x] **任务 1.7**: MCP Server 配置修复 (P1 Config)
    - `dimc mcp serve` 已支持 `stdio/http`
    - 非法 `--transport` 已直接报错，不再静默回退到 `stdio`
    - `tests/test_mcp.py`、`tests/test_mcp_server.py` 与 `tests/test_cli_mcp.py` 当前 `16/16` 通过
    - 后续若继续扩展，只剩特定客户端接入验证，不再是配置阻断
- [x] **任务 1.8**: 自动向量化 (Auto-Embedding) — Task 013 ✅ (2026-03-02)
        - SessionEndService._auto_embed_recent_events + VectorStore.add_batch
    - 解决 "收工后无法立即搜索" 的痛点

- [x] **Task 014**: 核心双P0修复 ✅ (2026-03-03)
    - VectorStore 写事务补齐 `BEGIN IMMEDIATE` 排他锁（消灭并发裸奔）
    - TUI 绕过 CausalEngine 直写图谱的越权强写链路物理铲除
    - `test_graph_store.py` GraphStore 导入断代修复

- [x] **Task 015**: CI净化与环境加固 ✅ (2026-03-04)
    - 清零 Ruff 历史 48 条错误（全仓 `src/` 全绿）
    - `scripts/check.zsh` 环境自动隔离加固（自动 `source .venv`）
    - 删除 `.pyc` 污染产物，修复重复测试行
    - 物理实测：Exit Code 0，24 passed，Ruff 0 errors
- [x] **Task 016**: 解释器因果引擎接入加深与纯化 ✅ (2026-03-04)
    - 图库彻底铲除 `nx.DiGraph` 基于内存图带来的同维度踩踏覆盖隐患，回退原生 SQLite 底座保障安全。
    - 引入结构体 `ORDER BY` 三重排序键保障防抖复现；切断 `LIKE` 模糊匹配根源。
- [x] **任务 1.9**: 发布文档与 release 清单核销 (P2 Docs)
    - `README.md` / `README_zh-CN.md` 已与 live 发布事实对齐
    - 当前未发布 PyPI，因此 README 已改为源码安装说明
    - `PyPI 发布` 继续单列在 Release 准备中跟踪，不再混在任务 1.9 里
- [x] **任务 3.0**: 遗留测试修复 (2026-02-19)
    - test_config (3), test_state (12), test_cli (3) → 56/56 passed
    - Bug Fix: `context.py` + `cli.py` orphan dict 访问
- [ ] **任务 3.1**: 全量测试红线清理 (The Big Cleanup)
    - 已完成第一阶段 (Prep): 通过 Marker 全局硬隔离
    - 已完成 skipped 债推进：清零 3 条过时 skipped + 1 条依赖缺失 skipped + 3 条 data_pipeline TODO skip + 2 条 fault_tolerance TODO skip，当前全量基线为 `1122 passed, 17 skipped, 4 deselected`
    - 第二阶段进行中：按依赖深度分批消化（浅层先行），继续补齐 `data_pipeline` / `fault_tolerance` / `llm_extractor`
- [x] **任务 3.2**: 静态分析配置基线修复 (P1 Config)
    - 仓库根目录已新增 `.pyre_configuration`
    - `source_directories/search_path` 已统一指向 `src`
    - `pyproject.toml` 中的 `mypy_path` 已对齐到 `src`
    - 当前仓库仍未内置 `pyre/pyre2/pyrefly` 可执行；运行级验证依赖开发机环境安装
    - 当前 live 类型检查器为 `mypy`，真实剩余债是类型错误本身，不再是 source root 配置
- [x] **信任梯度**: 契约模板引入 risk_level 分级 (P1 Process)
    - `task_packet.template.md` 已新增 `risk_level: low | medium | high`
    - `scheduler intake/kickoff` 已默认生成风险等级
    - `scheduler summary/closeout` 已按 `risk_level` 区分 `auto / manual_review / manual_approval`
- [ ] **L3 本体防波堤收口**: 持续压缩 SchemaValidator 的 legacy 生产面 (P0 Architecture)
    - 运行时卡口已接入 EventIndex 写入链路
    - 剩余主要是 legacy 类型治理、统计面与迁移闭环
- [ ] **L0 调度底座生产化收口**: 继续完善 Orchestrator 的生产化策略 (P1 Infrastructure)
    - 基础调度循环、branch/worktree/session bundle、`codex-run` 与 closeout 已实现
    - 剩余主要是策略收口、稳定性与更深生产化
- [ ] **L2.5 查询缓存层优化**: 在已落地 `events_cache` / `event_file_refs` 基础上继续做性能治理 (P1 Performance)
    - 剩余主要是 LRU 淘汰与更细粒度查询优化
- [x] **L1 自动化接入补齐**: IDE 环境自动探测与配置已落地 (P1 Data)
    - [x] 实现 `DirectoryImporter` 本地批量导入 (Task 010)
    - [x] 实现 `dimc detect` 与 `dimc config enable` IDE 环境自动探测
- [ ] **L4 推理引擎补齐**: 实现 Workflow 业务流程引擎 (P2 Reasoning)
    - 落地能够驱动复杂业务长跑的真实引擎
- [ ] **Release 准备**: PyPI 发布 + 最终发布核销
    - 当前 PyPI 上不存在 `dimcause` 可安装发行版
